---
title: §2.1 変数とデータ型
lastModified: '2025-05-06'
---

## 変数とデータ型

Python では「**変数**」を使ってデータを扱います。ここでは「**変数**」と「**データ型**」の概要について見ていきましょう。

### 1. 変数とは？

- **概要**: データを入れる「箱」に名前をつけたもの。
- **できること**: 数や文字、True/False を一時的に保存して、あとでプログラム内で使える。**変数名**は自由に決めることができる。

```python
age = 20
name = "Taro"
is_student = True

print(age)        # 20 を出力
print(name)       # Taro を出力
print(is_student) # True を出力
```

### 2. データ型とは？

- **概要**: コンピュータ上で扱うデータの種類を表すもの。
- **できること**: どんなデータを扱えるか、どんな操作ができるかを決める。

Pythonで使う主なデータ型は以下の4つです。

| 型        | 概要                     | できること                                      | 例             |
| --------- | ------------------------ | ----------------------------------------------- | -------------- |
| **int**   | 整数                     | 足し算・引き算などの算術計算                    | `x = 10`       |
| **float** | 小数                     | 小数を使った計算                                | `pi = 3.14`    |
| **str**   | 文字列（文字の並び）     | テキストを表示。文字列はダブルクォート`"`で囲む | `s = "hello"`  |
| **bool**  | True（真）か False（偽） | 条件判断や論理演算に利用                        | `flag = False` |

```python
x = 5           # int
y = 2.5         # float
s = "Python"    # str
flag = True     # bool
```

### 3. 型を確認する

- `type()` 関数で変数の型を調べられます。

```python
print(type(x))  # <class 'int'>
print(type(s))  # <class 'str'>
```
